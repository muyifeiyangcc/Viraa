import UIKit
import SnapKit
final class EULAController:UIViewController{
    override func viewDidLoad(){super.viewDidLoad();modalPresentationStyle = .overFullScreen;isModalInPresentation = true;view.backgroundColor = UIColor.black.withAlphaComponent(0.6);let panel = UIView();panel.backgroundColor = .white;panel.layer.cornerRadius = 24;view.addSubview(panel);panel.snp.makeConstraints{$0.leading.trailing.equalToSuperview();$0.bottom.equalToSuperview();$0.top.equalTo(view.safeAreaLayoutGuide).offset(40)};let title = UIFactory.label("EULA",size:28,weight:.bold);title.textAlignment = .center;let text = UITextView();text.isEditable = false;text.font = .systemFont(ofSize:15);text.text = "Welcome to Viraa. To create a positive, safe, and respectful space, do not share harmful, hateful, sexual, violent, misleading, dangerous, or unlawful content. Respect minors, personal privacy, public safety, other explorers, and the environment. Content and accounts that violate these rules may be restricted. By selecting Agree, you accept the Terms of Service and Privacy Policy.";let cancel = UIFactory.button("Cancel",background:AppTheme.surface,foreground:AppTheme.ink),agree = UIFactory.button("Agree",background:AppTheme.blue);cancel.addTarget(self,action:#selector(cancelTap),for:.touchUpInside);agree.addTarget(self,action:#selector(agreeTap),for:.touchUpInside);let row = UIStackView(arrangedSubviews:[cancel,agree]);row.spacing = 8;row.distribution = .fillEqually;panel.addSubview(title);panel.addSubview(text);panel.addSubview(row);title.snp.makeConstraints{$0.top.equalToSuperview().offset(34);$0.leading.trailing.equalToSuperview().inset(20)};row.snp.makeConstraints{$0.leading.trailing.equalToSuperview().inset(20);$0.bottom.equalTo(view.safeAreaLayoutGuide).inset(16);$0.height.equalTo(52)};text.snp.makeConstraints{$0.top.equalTo(title.snp.bottom).offset(12);$0.leading.trailing.equalToSuperview().inset(20);$0.bottom.equalTo(row.snp.top).offset(-16)}};@objc private func cancelTap(){exit(0)};@objc private func agreeTap(){AppRepository.shared.acceptEULA();dismiss(animated:true)}}
